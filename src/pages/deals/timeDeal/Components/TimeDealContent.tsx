import styled from "styled-components";

import { ContentDataProps } from "./Carousel";

export type Content = {content: ContentDataProps}

export const TimeDealContent = ({content} : Content) => {
    return(
        <></>
    )
}